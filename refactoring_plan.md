# Обновленный план рефакторинга с учетом всех замечаний

## Изменения в плане:

1. **Конфигурация .env**
   - Файл .env уже существует и содержит токен для VK
   - Все API-ключи (VK, aitunnel) будут в одном файле .env
   - Нет необходимости создавать новый файл конфигурации
   - Добавить все необходимые endpoints для aitunnel.ru
   - Добавить API-ключ для aitunnel.ru (значение оставлено пустым)

2. **Провайдер моделей**
   - Вместо proxyapi.ru использовать aitunnel.ru
   - Обновить конфигурацию RAG pipeline для работы с aitunnel
   - Изменить endpoint и API ключи в конфигурации
   - Использовать прямые HTTP-вызовы вместо SDK

3. **Фреймворк**
   - Заменить aiogram на vkbottle
   - Изучить документацию по vkbottle

## Обновленный план шагов:

### 1. **Анализ и подготовка**
- Изучить существующий .env файл для понимания структуры
- Изучить документацию aitunnel.ru для интеграции
- Изучить документацию vkbottle для понимания API
- Изучить файл config.py для понимания текущей конфигурации
- Проанализировать существующий код bot.py и кода в директории rag
- Определить структуру .env файла
- Проверить доступность всех endpoints aitunnel.ru
- Проверить доступность используемых ботом моделей на aitunnel.ru, при необходимости предложить замену

### 2. **Настройка зависимостей**
- Создать requirements_vk.txt с vkbottle
- Проверить совместимость с существующими зависимостями
- Обновить requirements.txt для production

### 3. **Рефакторинг кода**
- Обновить конфигурацию для aitunnel вместо proxyapi
- Создать scripts/vk_bot.py на основе существующего bot.py
- Заменить aiogram на vkbottle
- Сохранить всю логику RAG pipeline
- Использовать прямые HTTP-вызовы для aitunnel

### 4. **Конвертация обработчиков**
- Конвертировать все обработчики команд (/start, /help, /ask и т.д.)
- Адаптировать обработку изображений для VK API
- Сохранить всю бизнес-логику


## Ключевые изменения:

1. **API провайдер**: proxyapi.ru → aitunnel.ru
2. **Конфигурация**: Использовать существующий .env файл
3. **Фреймворк**: aiogram → vkbottle (без изменений в бизнес-логике)
4. **Интеграция с aitunnel**: прямые HTTP-вызовы
5. **Конфигурация**: изменения только в config.py
6. **Файл .env**: добавлены все необходимые endpoints и API-ключи

## Детализированный TODO-list по реализации:

### Подготовка
- [ ] Изучить документацию vkbottle
- [ ] Изучить документацию aitunnel.ru
- [ ] Определить доступные модели aitunnel.ru
- [ ] Изучить файл config.py
- [ ] Проанализировать существующий код в bot.py и директории rag
- [ ] Определить структуру .env файла
- [ ] Проверить доступность всех endpoints aitunnel.ru

### Настройка зависимостей
- [ ] Создать requirements_vk.txt с vkbottle
- [ ] Проверить совместимость с существующими зависимостями
- [ ] Обновить requirements.txt

### Рефакторинг кода
- [ ] Обновить конфигурацию в config.py, необходимые параметры кофигурации брать только из файла .env, имена глобальных переменных не менять
- [ ] При необходимости заменить модель для эмбеддингов в config.py на доступную через aitunnel.ru
- [ ] Добавить параметры конфигурации в файл .env
- [ ] Создать scripts/vk_bot.py
- [ ] Заменить aiogram на vkbottle
- [ ] Сохранить логику работы с RAG pipeline
- [ ] Использовать прямые HTTP-вызовы для aitunnel

#### Конвертация обработчиков
- [ ] Конвертировать /start
- [ ] Конвертировать /help
- [ ] Конвертировать /ask
- [ ] Конвертировать /ingest
- [ ] Конвертировать /stats
- [ ] Конвертировать /clear
- [ ] Конвертировать /test

### Обработка файлов и изображений
- [ ] Адаптировать обработку файлов и изображений для VK API

### RAG pipeline
- [ ] При необходимости, обновить RAG pipeline для VK и aitunnel.ru

